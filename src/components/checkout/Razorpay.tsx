/**
 * Razorpay.tsx — public alias for the demo Razorpay button used at checkout.
 * Wraps the existing RazorpayButton so the canonical filename matches the
 * payment.service.ts pairing.
 */
export { default } from "./RazorpayButton";
export { default as RazorpayButton } from "./RazorpayButton";
